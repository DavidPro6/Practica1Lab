<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1C1B2B"
    android:padding="24dp"
    tools:context=".QuizActivity">

    <TextView
        android:id="@+id/textViewPuntuacion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:text="Puntuación: 0"
        android:textAppearance="?attr/textAppearanceTitleMedium"
        android:textColor="#FFC107"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardViewPregunta"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        app:cardBackgroundColor="#0D47A1"
        app:cardCornerRadius="16dp"
        app:cardElevation="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/textViewPuntuacion"
        app:strokeColor="#42A5F5"
        app:strokeWidth="1dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:paddingTop="16dp"
            android:paddingBottom="16dp">

            <!-- ¡TEMPORIZADOR AÑADIDO AQUÍ! -->
            <TextView
                android:id="@+id/textViewTemporizador"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="15"
                android:textColor="@color/white"
                android:textSize="28sp"
                android:textStyle="bold"/>

            <TextView
                android:id="@+id/textViewPregunta"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:fontFamily="sans-serif-condensed-medium"
                android:gravity="center"
                android:minHeight="90dp"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:text="Cargando pregunta..."
                android:textAppearance="?attr/textAppearanceTitleLarge"
                android:textColor="@android:color/white"
                android:shadowColor="#000000"
                android:shadowDy="2"
                android:shadowRadius="4"
                android:textSize="20sp"/>

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- ImageView para la imagen de la pregunta (si la hay) -->
    <ImageView
        android:id="@+id/imageViewPregunta"
        android:layout_width="0dp"
        android:layout_height="180dp"
        android:layout_marginTop="16dp"
        android:scaleType="centerCrop"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/cardViewPregunta"
        tools:src="@tools:sample/avatars"
        tools:visibility="visible"/>

    <!-- Contenedor para los diferentes tipos de respuesta -->
    <FrameLayout
        android:id="@+id/containerRespuestas"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintBottom_toTopOf="@id/buttonConfirmarRespuesta"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/imageViewPregunta">

        <!-- 1. Opciones con RadioButton -->
        <RadioGroup
            android:id="@+id/radioGroupOpciones"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="gone">
            <RadioButton android:id="@+id/radioButtonOpcion1" style="@style/QuizRadioButton" android:text="Opción 1"/>
            <RadioButton android:id="@+id/radioButtonOpcion2" style="@style/QuizRadioButton" android:text="Opción 2"/>
            <RadioButton android:id="@+id/radioButtonOpcion3" style="@style/QuizRadioButton" android:text="Opción 3"/>
            <RadioButton android:id="@+id/radioButtonOpcion4" style="@style/QuizRadioButton" android:text="Opción 4"/>
        </RadioGroup>

        <!-- 2. Opciones con Imágenes (RecyclerView) -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerViewOpcionesImagen"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone" />

        <!-- 3. Opciones con ListView -->
        <ListView
            android:id="@+id/listViewOpciones"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@android:color/transparent"
            android:dividerHeight="8dp"
            android:visibility="gone" />

        <!-- 4. Opciones con Spinner -->
        <Spinner
            android:id="@+id/spinnerOpciones"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:background="@drawable/spinner_background"
            android:padding="16dp"
            android:visibility="gone" />

        <!-- El layout para imagen y texto libre ya no es necesario aquí -->

    </FrameLayout>

    <Button
        android:id="@+id/buttonConfirmarRespuesta"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:backgroundTint="#FFC107"
        android:padding="16dp"
        android:text="CONFIRMAR RESPUESTA"
        android:textColor="#1C1B2B"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
